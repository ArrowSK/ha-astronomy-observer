# Astronomy Observer Web

This directory prepares a standalone web deployment that is built from the same Astronomy Observer source tree as the Home Assistant app. It does not replace or modify the Home Assistant runtime.

The web build is intended for Docker-compatible hosts such as Railway. Railway deployment is deliberately not automatic; the repository only contains the build and service configuration.
