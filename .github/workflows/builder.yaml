name: Builder

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  build-app:
    name: Build Astronomy Observer
    permissions:
      contents: read
      id-token: write
      packages: write
    uses: ./.github/workflows/build-app.yaml
    with:
      app: astronomy_observer
      publish: ${{ github.event_name == 'push' }}
    secrets: inherit
